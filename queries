# Используемые запросы MySQL

Задание 1

Создание пользователя:
CREATE USER 'sys_temp'@'localhost' IDENTIFIED BY 'sys_temp';

Запрос на получение списка пользователей в базе данных:
SELECT user, host FROM mysql.user;

Команда выдачи всех прав для пользователя sys_temp:
GRANT ALL PRIVILEGES ON *.* TO 'sys_temp'@'localhost';

Создание базы данных sakila:
CREATE DATABASE sakila;

Импорт схемы базы данных sakila:
mysql -u sys_temp -p sakila < sakila-schema.sql

Импорт данных sakila:
mysql -u sys_temp -p sakila < sakila-data.sql

Выбор базы данных sakila:
USE DATABASE sakila;

Вывод списка таблиц базы данных sakila:
SHOW TABLES;

Задание 2

Запрос на выдачу данных для таблицы: название таблиц восстановленной базы данных и их первичных ключей:
mysql -u sys_temp -p -e "
SELECT t.TABLE_NAME, k.COLUMN_NAME AS PRIMARY_KEY
FROM information_schema.TABLES t
INNER JOIN information_schema.KEY_COLUMN_USAGE k ON t.TABLE_SCHEMA = k.TABLE_SCHEMA AND t.TABLE_NAME = k.TABLE_NAME AND k.CONSTRAINT_NAME = 'PRIMARY'
WHERE t.TABLE_SCHEMA = 'sakila'
ORDER BY t.TABLE_NAME, k.ORDINAL_POSITION;
" > keys2.txt
